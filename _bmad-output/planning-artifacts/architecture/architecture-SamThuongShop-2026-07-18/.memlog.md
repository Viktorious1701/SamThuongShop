---
scope: SamThuongShop full system: bilingual storefront+portfolio, checkout (physical+digital), orders, digital delivery, single-operator admin
purpose: Build substrate for AI-assisted solo build; feeds epics/stories/dev
altitude: System/initiative (whole product)
updated: 2026-08-05T02:23
---

- (constraint) Inputs: finalized PRD (25 FRs) + DESIGN.md + EXPERIENCE.md; inherit their decisions (bilingual VN/EN, made-to-order prints + digital downloads, VN local payments Momo/ZaloPay/bank/COD, email delivery, single operator, responsive web light-mode)
- (constraint) Build mode: solo intermediate operator building WITH AI assistance (BMad dev flow) -> favor mainstream, well-documented, AI-fluent stack
- (direction) Working mode = Coaching; deliverable = spine only
- (override) uv not installed; memlog via python3
- (decision) Paradigm+stack: Next.js 16 App Router full-stack monolith (portfolio+shop+admin one codebase). Seed: Postgres + Prisma 7, next-intl, Resend email, Cloudflare R2. Prevents: divergent framework/data choices across units
- (version) Next.js 16.2.x (App Router, Active LTS), Prisma 7, next-intl, Resend, Cloudflare R2, payOS Node SDK (verified current 2026-07)
- (decision) Payments: payOS (VietQR bank-transfer) + COD as order-state. Payment provider behind a common interface so Momo/ZaloPay wallets (VNPay/OnePay, needs registered business) can be added later without rework. Prevents: hardcoding one gateway; trusting client for payment success
- (constraint) Indie-merchant constraint: Momo/ZaloPay WALLET balances require registered business + VNPay/OnePay contract; not available at launch. VietQR+COD only for v1
- (question) PRD DIVERGENCE: PRD lists Momo/ZaloPay/bank/COD as accepted; architecture restricts v1 to VietQR(payOS)+COD. Offer to update PRD FR-8/FR-9 + assumptions at finalize
- (decision) Auth = Auth.js v5 (NextAuth), self-hosted email/password in Postgres. Single operator role + optional customer accounts. Prevents: scattered/ad-hoc auth, external data-residency dependency
- (decision) Hosting = Vercel (Next.js) + Neon Postgres, both pinned to Singapore (sin1) region for VN latency. Cloudflare R2 for asset storage; Resend for transactional email. Prevents: divergent deploy/runtime targets
- (decision) Order state machine (ADOPTED from PRD): Pending Payment->Paid->Processing->Shipped->Completed, +Cancelled. COD placed as Processing; digital-only ->Completed on link issue. Paid set ONLY by verified payOS webhook. Binds: order lifecycle. Prevents: client-trusted payment, ad-hoc statuses
- (decision) Money = integer VND (minor-unit-less dong) across DB, logic, API, display. Prevents: float rounding drift
- (decision) Digital asset protection: originals in PRIVATE R2 bucket; delivered only via short-lived presigned URLs minted after Order Paid; watermarked preview is a separate derivative generated at upload; original never served pre-purchase. Binds SM-C2 counter-metric. Prevents: leak of un-watermarked files
- (decision) i18n: translatable content stored per-locale (vi/en) fields on Product/Portfolio/About; Vietnamese fallback; next-intl for UI strings; prices VND-only (no currency dimension). Prevents: single-language schema, translation drift
- (decision) Variant carries format (physical print size | digital tier); format drives fulfillment: one Order may hold both; shipping applies iff any physical line; COD disabled iff any digital line. Prevents: inconsistent physical/digital handling
- (decision) Mutation model: state changes via Server Actions; webhooks (payOS) + protected download + auth callbacks via Route Handlers; Server Components default, client components only for interactive UI. Prevents: divergent data-fetch/mutation patterns
- (decision) Module boundary: feature-oriented under app/ (routes) with a server-only data/service layer (lib/server) owning all DB (Prisma) access + domain logic; UI components never touch the DB directly. Prevents: business logic leaking into components, N+1 ad-hoc queries
- (constraint) Transactional email (Resend): order confirmation, digital Download Link, status updates all go through one email-sending service module
- (assumption) Basic Product search = Postgres full-text/ILIKE on name (per PRD FR-3, no faceted search)
- (assumption) Shipping = flat fee configured by Operator (PRD Open Q2 unresolved); payment layer + order totals treat it as a single configurable line
- (direction) Deferred (named, not decided): Momo/ZaloPay wallets (needs business reg + VNPay/OnePay); advanced search/analytics; CI specifics; rate-limiting/advanced observability (Vercel+Neon defaults for v1); exact print sizes/digital tiers (PRD Open Q4)
- (event) Spine distilled: 9 ADs, conventions, stack, structural seed (3 mermaid), capability map, deferred
- (decision) AD-10: Order persists decomposed integer-VND subtotal/shipping/discount/grandTotal; grandTotal is sole amount to PaymentProvider; shipping is Order field not line; server recomputes, never trusts client total
- (decision) AD-11: OrderLine snapshots unitPrice+format+locale-name at creation; historical reads never live-join Variant (prevents operator edits re-pricing old orders)
- (decision) AD-12: DownloadGrant issued for every digital line at Paid (not Completed); headline status tracks physical track; digital-only short-circuits to Completed; per-track cancellation
- (decision) AD-13: Order-state transitions atomic+idempotent: unique payOS ref on Payment, upsert-no-op, compare-and-set monotonic transitions (Paid wins vs Cancel->refund), once-per-(order,purpose) grant/email dispatch
- (decision) AD-14: DownloadGrant = durable entitlement minting short-lived presigned URLs under expiry/count policy; guest redemption requires order-ref + purchaser email + rate limiting (closes enumeration hole)
- (decision) AD-15: Two-tier R2 storage (private originals vs public display/preview); non-derivable random keys owned by DB record; preview never reveals original
- (decision) Resolved: locale-prefixed URLs (/vi,/en; vi default) + per-locale OG metadata (settles EXPERIENCE OG/URL open question); AD-8 adds digital qty=1; email invariant added to AD-3/AD-6 + Email convention
- (constraint) Version review: Auth.js v5 is BETA (next-auth@beta) - noted in stack/AD-9; payOS has NO public sandbox - dev mocks PaymentProvider; Vercel sin1 pin needs Pro plan
- (event) Reviewer gate applied: lint clean; adversarial 4C+4H closed via AD-10..AD-15 + AD-3/6/7/8 tightening; version fixes; input-reconcile gaps (qty=1, media, SEO, email, payment-divergence) closed
- (event) spine finalized
- (event) Synced PRD + EXPERIENCE to payOS(VietQR)+COD; Momo/ZaloPay wallets now framed as deferred post-launch in all three docs
- (decision) DB provider changed Neon -> Supabase Postgres (Singapore ap-southeast-1, PG 17.6) per user choice. Driver adapter @prisma/adapter-neon -> @prisma/adapter-pg; app runtime uses transaction pooler (6543, DATABASE_URL), migrations use session pooler (5432, DIRECT_URL). Spine Stack/environments updated
